def n_grammer(text, num=3):
    temp = []
    my_list = []

    if (len(my_list) >= num):
        temp.append(int(my_list[0:num]))
        for i in range(num):
            text.split("")
    else:
        temp.append(num - len(text) * 0)
